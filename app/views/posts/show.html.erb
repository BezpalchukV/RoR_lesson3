<div class="container">
<div class="row">
<div class="col-sm-8 blog-main">
  <div>
    <%= image_tag avatar_url(@post.user), height: '64', width: '64' %>
  </div>
  <div class="blog-post">
    <p id="notice" role="alert"><%= notice %></p>
    <p>
      <strong>Title:</strong>
      <h2 class="blog-post-title"><%= @post.title %></h2>
    </p>

    <p>
      <strong>Body:</strong><br>
      <%= @post.body %>
    </p>

    <p>
      <strong>Tags:</strong><br>
      <%= @post.tags %>
    </p>

    <p>
      <%= get_likes(@post, 1) %>
      <%= link_to "Like", vote_post_path(value: 'like'), method: :put, class: 'btn btn-success rate' %>
      <%= link_to "Dislike", vote_post_path(vlaue: 'dislike'), method: :put, class: 'btn btn-danger rate' %>
      <%= get_likes(@post, -1) %>
    </p>

    <p class="blog-post-meta">
       <strong>Created by: </strong><%= @post.user.name %>
    </p>
  </div>
  <% if @post.user.eql?(current_user) %>
    <%= link_to 'Edit', edit_post_path(@post), :class => 'btn btn-default btn-sm' %>
  <% end %>
  <%= link_to 'Back', posts_path, :class => 'btn btn-default btn-sm' %>
  <br><br><br>

  <div clas="well">
      <% if current_user %>
          <%= form_for [@post, Comment.new], :html => {:role=> 'form'} do |f| %>
              <p>
                <%= f.label :body, "New comment" %><br/>
                <div class="form-group">
                    <%= f.text_area :body, class: 'form-control'%>
                </div>
              </p>
              <p><%= f.submit "Add comment", class: 'btn btn-primary' %></p>
          <% end %>
      <% end %>
  </div>
  <hr>
  <h3>Comments:</h3>

  <%= render :partial => @post.comments %>


</div>
</div>
</div>

